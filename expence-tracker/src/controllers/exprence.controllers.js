/**
 * add new expence
 * viewAll exprence
 * view preticuler expence
 * update expence
 * remove expence
 * 
 */


// controller for add new expence
export const addNewExpence = AsyncHandler(async(req, res) => {
  
})

// controller for view all expence
export const viewAllExpence = AsyncHandler(async(req, res) => {
  
})

// controller for view perticuler expence
export const viewExpence = AsyncHandler(async(req, res) => {
  
})

// controller for update exprence
export const updateExpence = AsyncHandler(async(req, res) => {
  
})

// controller for remove expence
export const removeExpence = AsyncHandler(async(req, res) => {
  
})