/**
 * register user
 * login user
 * logout user
 * update user details
 * change Password
 * reset Password
 * delete user
 */

import AsyncHandler from "../utils/asyncHandler.js"
import UserModel from "../models/user.models.js"

// controller for registerUser
export const registerUser = AsyncHandler(async(req, res) => {

})

// controller for loginUser
export const loginUser = AsyncHandler(async(req, res) => {
  
})

// controller for logoutUser
export const logoutUser = AsyncHandler(async(req, res) => {
  
})

// controller for updateUserDetails
export const updateUserDetails = AsyncHandler(async(req, res) => {
  
})

// controller for Change UserPassword
export const changeUserPassword = AsyncHandler(async(req, res) => {
  
})

// controller for Reset UserPassword
export const resetUserPassword = AsyncHandler(async(req, res) => {
  
})

// controller for deleteUser
export const deleteUser = AsyncHandler(async(req, res) => {
  
})
